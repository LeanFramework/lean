{
    "require": {
        "phpunit/phpunit": "dev-master"
    },
    "autoload": {
        "psr-4": { "Lean\\" : "src/" }
    },
    "scripts": {
        "test": "phpunit --colors=always --bootstrap=vendor/autoload.php tests",
        "docker-test": "docker exec lean_web_1 php /var/www/vendor/bin/phpunit --bootstrap=/var/www/vendor/autoload.php /var/www/tests"
    }
}
